import { Typography } from "@mui/material";

const Profile = () => {
  return (
    <section>
        <Typography
            variant="h4"
            component="h1"
            children="Profile"
        />
    </section>
  )
}

export default Profile;
